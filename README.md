# Homeward_1


GitHub Befehle

git pull  // Standart um GitHub Version zu holen

//wenn pull alleien nichts klappt. Mit diesem befehl lokale sachen löschen und GitHub Version verwenden (lokale Version wird überschrieben und die aktuelle GitHub Version verwendet). 
git fetch origin
git reset --hard origin/main


// Standart Befehl um neue Version hochzuladen
git add .
git commit -m "text"
git push
